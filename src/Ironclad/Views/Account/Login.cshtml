@using System.Collections.Generic
@using System.Linq
@using Microsoft.AspNetCore.Http
@using Microsoft.AspNetCore.Http.Authentication
@model LoginModel
@inject SignInManager<ApplicationUser> SignInManager
@inject WebsiteSettings websiteSettings

@{
    ViewData["Title"] = "Log in";
}

<header class="header">
    <div class="container-fluid">
        <a href="~/" class="navbar-brand">
            <img src="~/@websiteSettings.Logo" class="d-none d-md-inline-block" width="132">
            <img src="~/@websiteSettings.DarkLogo" class="d-inline-block d-md-none" width="132">
        </a>
    </div>
</header>
<div class="content row justify-content-center">
    <div class="content_left col-md-5 col-xl-4 d-none d-md-flex"
         style="background-image: url(img/login_bg.jpg)">
        <div class="content_left__hint align-self-end">
            "Donec facilisis tortor ut augue lacinia, at viverra est semper.
            Sed ss, scelerisque nec pharetra id, tem tortorlentesque non nissim neque.
            Ut porta viverra est, ut dignissim elit elementum ut."
        </div>
    </div>
    <div class="content_right col-11 col-md-7 col-xl-8 text-center">
        <div class="row align-content-md-center justify-content-center">
            <div class="col-md-11">
                <div class="content_header">
                    <h1 class="h2">Welcome back</h1>
                    <p class="lead">Enter your details below</p>
                </div>
                <section class="content_form">
                    <form asp-route-returnurl="@ViewData["ReturnUrl"]" method="post">
                        <div class="form-group">
                            <label asp-for="Username" class="control-label"></label>
                            <input asp-for="Username" class="form-control" placeholder="Enter your email" />
                            <span asp-validation-for="Username" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="Password" class="control-label"></label>
                            <div class="input-group-alt">
                                <input asp-for="Password" class="form-control" placeholder="Enter your password" />
                                <button class="btn btn-icon btn-append" type="button">
                                    <i class="icon icon--show"></i>
                                </button>
                            </div>
                            <span asp-validation-for="Password" class="text-danger"></span>
                        </div>
                        <div class="row justify-content-between">
                            <div class="col-auto">
                                <div class="form-group">
                                    <div class="custom-control custom-checkbox">
                                        <input type="checkbox" class="custom-control-input" asp-for="RememberMe">
                                        <label class="custom-control-label" asp-for="RememberMe">
                                            @Html.DisplayNameFor(m => m.RememberMe)
                                        </label>
                                    </div>
                                </div>
                            </div>
                            <div class="col-auto">
                                <div class="form-group">
                                    <a asp-action="ForgotPassword" class="request-link">Forgot password?</a>
                                </div>
                            </div>
                        </div>
                        <div class="row justify-content-center">
                            <div class="col-sm-9">
                                <div class="form-group">
                                    <button type="submit" class="btn btn-gradient btn-block btn-lg">Sign In</button>
                                </div>
                            </div>
                        </div>
                        <p class="muted text-center">
                            Don't have an account?
                            <a asp-action="Register" asp-route-returnurl="@ViewData["ReturnUrl"]">
                                Sign Up
                            </a>
                        </p>
                    </form>
                    <div class="form-group text-center">
                        @{
                            var loginProviders = (await SignInManager.GetExternalAuthenticationSchemesAsync()).ToList();
                            if (loginProviders.Count != 0)
                            {
                                <p>
                                    Or using
                                </p>
                                <form asp-action="ExternalLogin" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post" class="form-horizontal">
                                    <div>
                                        <p>
                                            @foreach (var provider in loginProviders)
                                            {
                                                <button type="submit" class="btn btn-primary external-login-button" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account">@provider.DisplayName</button>
                                            }
                                        </p>
                                    </div>
                                </form>
                            }
                        }
                    </div>
                </section>
            </div>
        </div>
    </div>
</div>